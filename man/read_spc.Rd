% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_spc.R
\name{read_spc}
\alias{read_spc}
\title{Read and format spectral data from a file}
\usage{
read_spc(file, sep = "\t", dec = ".", header = TRUE, spectra_prefix = "",
         spectra_starts = NA, spectra_ends = NA, ...)
}
\arguments{
\item{file}{a character string specifying the path to the file containing the spectral data.}

\item{sep}{a character string indicating the field separator character. Defaults to \code{"\t"}.}

\item{dec}{a character string used for decimal points. Defaults to \code{"."}.}

\item{header}{logical value indicating whether the file contains the names
of the variables as its first line. Defaults to \code{TRUE}}

\item{spectra_prefix}{a character string specifying the prefix used for spectral column names. If empty, the function will use column indices instead.}

\item{spectra_starts}{an integer indicating the starting column index for the spectral data, used when \code{spectra_prefix} is not specified.}

\item{spectra_ends}{an integer indicating the ending column index for the spectral data, used when \code{spectra_prefix} is not specified. If not provided, defaults to the last column.}

\item{...}{additional arguments passed to \code{\link[utils]{read.table}}.}
}
\value{
a data frame with the original data and a matrix of spectral data
stored in the \code{spc} column.
}
\description{
This function reads spectral data from a file and extracts the spectral columns
based on a specified prefix, or a range of columns. It can handle various delimiters
and decimal separators.
}
\details{
The function reads a file and extracts the spectral data based on either a
column name prefix or specified column indices. The spectral data is returned
as a matrix in the \code{spc} column of the resulting data frame.
}
\examples{
\donttest{
# write a file with spectra
data("NIRsoil", package = "prospectr")
spc_small <- NIRsoil$spc[1:5, ]
colnames(spc_small) <- paste0("X", colnames(spc_small))
tmp_df <- data.frame(ID = 1:5, Nt = NIRsoil$Nt[1:5], spc_small, check.names = FALSE)
tmp_file <- tempfile(fileext = ".txt")
write.table(tmp_df, file = tmp_file, sep = "\t", row.names = FALSE)

# read that
result <- read_spc(tmp_file, spectra_prefix = "X")
}

}
\author{
Leonardo Ramirez-Lopez
}
